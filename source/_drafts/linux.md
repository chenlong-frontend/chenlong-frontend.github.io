两次 tab 显示补全选项

```
cd - // 进入上次操作目录
cp // 复制文件
cp -r // 复制文件夹
ln -s // 创建软连接，源文件写绝对路径
whereis(which) // 搜索指令所在目录
find /home -name "?s" // 模糊查询
-exec ls -l {} \ // -exec 指令 {} \ 可对第一次结果执行指令
grep // 搜索文件内容
man // 查看指令帮助文档
```
